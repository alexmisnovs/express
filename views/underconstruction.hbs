    {{> header}}
    <h1> {{ pageTitle }} </h1>
   
    <div class="row">
        {{message}}
    </div>
    <ul>
        {{#each this.array}}
            <li>{{ this  }}</li>
        {{/each}}
    </ul>
   {{> footer}}